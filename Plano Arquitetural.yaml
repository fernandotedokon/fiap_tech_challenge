
---

## 🧭 Plano Arquitetural (Pipeline e Escalabilidade)

### 🔄 Pipeline da Solução

```plaintext
      ┌─────────────┐
      │ books.toscrape.com
      └──────┬──────┘
             │  (requests + BS4)
             ▼
      ┌────────────────┐
      │ Scraper Python │
      │ scraper.py     │
      └──────┬─────────┘
             ▼  (CSV: pandas)
      ┌────────────────────┐
      │  data/books.csv    │
      └──────┬─────────────┘
             ▼
      ┌────────────────────┐
      │   FastAPI backend  │
      │   main.py + models │
      └──────┬─────────────┘
             ▼
      ┌────────────────────┐
      │  REST Endpoints    │
      └──────┬─────────────┘
             ▼
     ┌────────────────────────┐
     │   Cientistas de Dados  │
     │   Frontends / ML / BI  │
     └────────────────────────┘


